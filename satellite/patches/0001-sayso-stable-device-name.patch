--- a/linux_voice_assistant/__main__.py
+++ b/linux_voice_assistant/__main__.py
@@ -46,6 +46,10 @@ async def main() -> None:
         "--name",
         help="Real name for the device",
     )
+    parser.add_argument(
+        "--device-name",
+        help="Stable device identifier for Home Assistant (default: lva-<mac>)",
+    )
     parser.add_argument(
         "--audio-input-device",
         help="Name for the audio input device (see --list-input-devices)",
@@ -314,6 +318,8 @@ async def main() -> None:
         friendly_name = args.name
 
     device_name = f"lva-{mac_address_clean}"
+    if args.device_name:
+        device_name = args.device_name
 
     print(f"Device name: {device_name}")
