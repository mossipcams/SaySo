# Corpus generation, data views, and training-repo tests (CPU; no torch, no trainer).
# The trainer lives in its host container; this venv never needs torch.
# Host venv: /srv/llm/sayso/training/.venv on `ssh llm`. See README.md.
hassil==3.12.1
jsonschema==4.26.0
PyYAML==6.0.3
transformers==5.6.0
tokenizers==0.22.2
Jinja2==3.1.6
requests>=2.32.0
hypothesis==6.168.0
pytest==9.1.1
